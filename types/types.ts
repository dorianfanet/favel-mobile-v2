export type UserMetadata = {
  id: string;
  firstName?: string;
  lastName?: string;
  email?: string;
  createdAt?: string;
  avatarUrl?: string;
};

export type JsonCoordinates = {
  lat: number;
  lng: number;
};
