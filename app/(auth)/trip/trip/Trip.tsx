import { View, Text } from "react-native";
import React from "react";

export default function Trip() {
  return (
    <View
      style={{
        position: "absolute",
      }}
    >
      <Text>Trip</Text>
    </View>
  );
}
